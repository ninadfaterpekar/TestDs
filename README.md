# TestDs

## Angular app scaffold

I scaffolded a new Angular app in `angular-app` using the Angular CLI.

- Project path: `angular-app`
- To develop locally: `cd angular-app && npm start` (runs the dev server)
- To build production: `cd angular-app && npm run build`

Built production files are placed in `angular-app/dist/angular-app`.

If you want me to commit these files or change the app name/config, tell me and I'll update the repo.
Just testing reliability DS
